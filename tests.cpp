#include "tests.h"

void test_nearest_neighbours()
{
    // define some points
    Eigen::Matrix<double, 5, 3> points;
    points <<   -1.5, -2.5, -3.2,
                1.26, -4.5, 5.1,
                -1.1, -9.0, 2.3,
                9.9, 8.2, -3.0,
                3.8, -7.2, 0.0;


    // test to make sure we aren't asking for more points than actually exist!
    

    // compute nearest neighbours


    // assert that we get the expected response
}